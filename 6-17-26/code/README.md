# Code

`original_scripts/` preserves the scripts from the working investigation. They are useful for reconstructing how tables were generated, but they reflect an iterative research state.

For manuscript-grade reruns, create new locked reproduction scripts that consume a frozen prompt manifest and write the provenance schema described in `../docs/PROVENANCE_AND_MANIFEST_SCHEMA.md`.

